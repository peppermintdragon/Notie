version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: twogether-db
    restart: unless-stopped
    environment:
      POSTGRES_DB: twogether
      POSTGRES_USER: twogether
      POSTGRES_PASSWORD: twogether_dev_password
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
